1. install virtualenv:
    MAC- 
    WIN- py -m py pip install virtualenv
    --using 'py' python i want to use the modules, the one that i want to use the 'pip', and i want to install 'virtualenv'

2. Use venv to create a virtual environment:
    MAC- python3 -m venv venv
    WIN- py -m venv venv

3. Activate/Launch the virtual environment:
    MAC- source venv/bin/activate
    WIN- venv\Scripts\activate

4. Install the required packages using pip:
    MAC- pip install -r requirements.txt, python3 -m pip install flask
    WIN- pip install -r requirements.txt, py -m pip install flask
    --using 'pip' to install the packages, and 'requirements.txt' is the file that contains the list of packages to be installed.
    --using 'py' to install the packages, and 'flask' is the package that i want to install.
    --using 'python3' to install the packages, and 'flask' is the packages

    